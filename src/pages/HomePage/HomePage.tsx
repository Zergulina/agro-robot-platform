import React from 'react';
import pageClasses from '../Page.module.css';

const HomePage: React.FC = () => {
    return (
        <main className={pageClasses.Page}>
            
        </main>
    );
};

export default HomePage;